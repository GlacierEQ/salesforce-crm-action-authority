[project]
name = "salesforce-crm-action-authority"
version = "0.1.0"
description = "Deterministic CRM action authority with scoped grants and reverse-operation receipts"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
