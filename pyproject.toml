[project]
name = "salesforce-crm-action-authority"
version = "0.0.1"
description = "Scaffold: CRM Action Authority (independent Salesforce lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
